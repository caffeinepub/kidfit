import { c as createLucideIcon, r as reactExports, j as jsxRuntimeExports, u as ue } from "./index-BtWcNydt.js";
import { B as Button } from "./button-CjFCga6J.js";
import { D as Dialog, a as DialogContent, b as DialogHeader, c as DialogTitle, X } from "./dialog-Bp2rno-3.js";
import { C as Check } from "./check-CigWu6RL.js";
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$8 = [
  [
    "path",
    {
      d: "M14.5 4h-5L7 7H4a2 2 0 0 0-2 2v9a2 2 0 0 0 2 2h16a2 2 0 0 0 2-2V9a2 2 0 0 0-2-2h-3l-2.5-3z",
      key: "1tc9qg"
    }
  ],
  ["circle", { cx: "12", cy: "13", r: "3", key: "1vg3eu" }]
];
const Camera = createLucideIcon("camera", __iconNode$8);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$7 = [
  ["circle", { cx: "12", cy: "12", r: "10", key: "1mglay" }],
  ["line", { x1: "12", x2: "12", y1: "8", y2: "12", key: "1pkeuh" }],
  ["line", { x1: "12", x2: "12.01", y1: "16", y2: "16", key: "4dfq90" }]
];
const CircleAlert = createLucideIcon("circle-alert", __iconNode$7);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$6 = [
  ["path", { d: "M21.801 10A10 10 0 1 1 17 3.335", key: "yps3ct" }],
  ["path", { d: "m9 11 3 3L22 4", key: "1pflzl" }]
];
const CircleCheckBig = createLucideIcon("circle-check-big", __iconNode$6);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$5 = [
  ["rect", { width: "14", height: "14", x: "8", y: "8", rx: "2", ry: "2", key: "17jyea" }],
  ["path", { d: "M4 16c-1.1 0-2-.9-2-2V4c0-1.1.9-2 2-2h10c1.1 0 2 .9 2 2", key: "zix9uf" }]
];
const Copy = createLucideIcon("copy", __iconNode$5);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$4 = [
  ["rect", { width: "20", height: "20", x: "2", y: "2", rx: "5", ry: "5", key: "2e1cvw" }],
  ["path", { d: "M16 11.37A4 4 0 1 1 12.63 8 4 4 0 0 1 16 11.37z", key: "9exkf1" }],
  ["line", { x1: "17.5", x2: "17.51", y1: "6.5", y2: "6.5", key: "r4j83e" }]
];
const Instagram = createLucideIcon("instagram", __iconNode$4);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$3 = [
  ["path", { d: "M7.9 20A9 9 0 1 0 4 16.1L2 22Z", key: "vv11sd" }]
];
const MessageCircle = createLucideIcon("message-circle", __iconNode$3);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$2 = [
  ["path", { d: "M3 12a9 9 0 0 1 9-9 9.75 9.75 0 0 1 6.74 2.74L21 8", key: "v9h5vc" }],
  ["path", { d: "M21 3v5h-5", key: "1q7to0" }],
  ["path", { d: "M21 12a9 9 0 0 1-9 9 9.75 9.75 0 0 1-6.74-2.74L3 16", key: "3uifl3" }],
  ["path", { d: "M8 16H3v5", key: "1cv678" }]
];
const RefreshCw = createLucideIcon("refresh-cw", __iconNode$2);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode$1 = [
  ["path", { d: "M3 12a9 9 0 1 0 9-9 9.75 9.75 0 0 0-6.74 2.74L3 8", key: "1357e3" }],
  ["path", { d: "M3 3v5h5", key: "1xhq8a" }]
];
const RotateCcw = createLucideIcon("rotate-ccw", __iconNode$1);
/**
 * @license lucide-react v0.511.0 - ISC
 *
 * This source code is licensed under the ISC license.
 * See the LICENSE file in the root directory of this source tree.
 */
const __iconNode = [
  ["circle", { cx: "18", cy: "5", r: "3", key: "gq8acd" }],
  ["circle", { cx: "6", cy: "12", r: "3", key: "w7nqdw" }],
  ["circle", { cx: "18", cy: "19", r: "3", key: "1xt0gg" }],
  ["line", { x1: "8.59", x2: "15.42", y1: "13.51", y2: "17.49", key: "47mynk" }],
  ["line", { x1: "15.41", x2: "8.59", y1: "6.51", y2: "10.49", key: "1n3mei" }]
];
const Share2 = createLucideIcon("share-2", __iconNode);
const useCamera = (config = {}) => {
  const {
    facingMode = "environment",
    width = 1920,
    height = 1080,
    quality = 0.8,
    format = "image/jpeg"
  } = config;
  const [isActive, setIsActive] = reactExports.useState(false);
  const [isSupported, setIsSupported] = reactExports.useState(null);
  const [error, setError] = reactExports.useState(null);
  const [isLoading, setIsLoading] = reactExports.useState(false);
  const [currentFacingMode, setCurrentFacingMode] = reactExports.useState(facingMode);
  const videoRef = reactExports.useRef(null);
  const canvasRef = reactExports.useRef(null);
  const streamRef = reactExports.useRef(null);
  const isMountedRef = reactExports.useRef(true);
  reactExports.useEffect(() => {
    var _a;
    const supported = !!((_a = navigator.mediaDevices) == null ? void 0 : _a.getUserMedia);
    setIsSupported(supported);
  }, []);
  reactExports.useEffect(() => {
    return () => {
      isMountedRef.current = false;
      cleanup();
    };
  }, []);
  const cleanup = reactExports.useCallback(() => {
    if (streamRef.current) {
      for (const track of streamRef.current.getTracks()) {
        track.stop();
      }
      streamRef.current = null;
    }
    if (videoRef.current) {
      videoRef.current.srcObject = null;
    }
    setIsActive(false);
  }, []);
  const createMediaStream = reactExports.useCallback(
    async (facing) => {
      try {
        const constraints = {
          video: {
            facingMode: facing,
            width: { ideal: width },
            height: { ideal: height }
          }
        };
        const stream = await navigator.mediaDevices.getUserMedia(constraints);
        if (!isMountedRef.current) {
          for (const track of stream.getTracks()) {
            track.stop();
          }
          return null;
        }
        return stream;
      } catch (err) {
        let errorType = "unknown";
        let errorMessage = "Failed to access camera";
        if (err.name === "NotAllowedError") {
          errorType = "permission";
          errorMessage = "Camera permission denied";
        } else if (err.name === "NotFoundError") {
          errorType = "not-found";
          errorMessage = "No camera device found";
        } else if (err.name === "NotSupportedError") {
          errorType = "not-supported";
          errorMessage = "Camera is not supported";
        }
        throw { type: errorType, message: errorMessage };
      }
    },
    [width, height]
  );
  const setupVideo = reactExports.useCallback(async (stream) => {
    if (!videoRef.current) return false;
    const video = videoRef.current;
    video.srcObject = stream;
    return new Promise((resolve) => {
      const onLoadedMetadata = () => {
        video.removeEventListener("loadedmetadata", onLoadedMetadata);
        video.removeEventListener("error", onError);
        video.play().catch((err) => {
          console.warn("Video autoplay failed:", err);
        });
        resolve(true);
      };
      const onError = () => {
        video.removeEventListener("loadedmetadata", onLoadedMetadata);
        video.removeEventListener("error", onError);
        resolve(false);
      };
      video.addEventListener("loadedmetadata", onLoadedMetadata);
      video.addEventListener("error", onError);
      if (video.readyState >= 1) {
        onLoadedMetadata();
      }
    });
  }, []);
  const startCamera = reactExports.useCallback(async () => {
    if (isSupported === false || isLoading) {
      return false;
    }
    setIsLoading(true);
    setError(null);
    try {
      cleanup();
      const stream = await createMediaStream(currentFacingMode);
      if (!stream) return false;
      streamRef.current = stream;
      const success = await setupVideo(stream);
      if (success && isMountedRef.current) {
        setIsActive(true);
        return true;
      }
      cleanup();
      return false;
    } catch (err) {
      if (isMountedRef.current) {
        setError(err);
      }
      cleanup();
      return false;
    } finally {
      if (isMountedRef.current) {
        setIsLoading(false);
      }
    }
  }, [
    isSupported,
    isLoading,
    currentFacingMode,
    cleanup,
    createMediaStream,
    setupVideo
  ]);
  const stopCamera = reactExports.useCallback(async () => {
    if (isLoading) return;
    setIsLoading(true);
    cleanup();
    setError(null);
    await new Promise((resolve) => setTimeout(resolve, 100));
    if (isMountedRef.current) {
      setIsLoading(false);
    }
  }, [isLoading, cleanup]);
  const switchCamera = reactExports.useCallback(
    async (newFacingMode) => {
      if (isSupported === false || isLoading) {
        return false;
      }
      const targetFacingMode = newFacingMode || (currentFacingMode === "user" ? "environment" : "user");
      setIsLoading(true);
      setError(null);
      try {
        cleanup();
        setCurrentFacingMode(targetFacingMode);
        await new Promise((resolve) => setTimeout(resolve, 100));
        const stream = await createMediaStream(targetFacingMode);
        if (!stream) return false;
        streamRef.current = stream;
        const success = await setupVideo(stream);
        if (success && isMountedRef.current) {
          setIsActive(true);
          return true;
        }
        cleanup();
        return false;
      } catch (err) {
        if (isMountedRef.current) {
          setError(err);
        }
        cleanup();
        return false;
      } finally {
        if (isMountedRef.current) {
          setIsLoading(false);
        }
      }
    },
    [
      isSupported,
      isLoading,
      currentFacingMode,
      cleanup,
      createMediaStream,
      setupVideo
    ]
  );
  const retry = reactExports.useCallback(async () => {
    if (isLoading) return false;
    setError(null);
    await stopCamera();
    await new Promise((resolve) => setTimeout(resolve, 200));
    return startCamera();
  }, [isLoading, stopCamera, startCamera]);
  const capturePhoto = reactExports.useCallback(() => {
    return new Promise((resolve) => {
      if (!videoRef.current || !canvasRef.current || !isActive) {
        resolve(null);
        return;
      }
      const video = videoRef.current;
      const canvas = canvasRef.current;
      canvas.width = video.videoWidth;
      canvas.height = video.videoHeight;
      const ctx = canvas.getContext("2d");
      if (!ctx) {
        resolve(null);
        return;
      }
      if (currentFacingMode === "user") {
        ctx.scale(-1, 1);
        ctx.drawImage(video, -canvas.width, 0);
      } else {
        ctx.drawImage(video, 0, 0);
      }
      canvas.toBlob(
        (blob) => {
          if (blob) {
            const extension = format.split("/")[1];
            const file = new File([blob], `photo_${Date.now()}.${extension}`, {
              type: format
            });
            resolve(file);
          } else {
            resolve(null);
          }
        },
        format,
        quality
      );
    });
  }, [isActive, format, quality, currentFacingMode]);
  return {
    // State
    isActive,
    isSupported,
    error,
    isLoading,
    currentFacingMode,
    // Actions
    startCamera,
    stopCamera,
    capturePhoto,
    switchCamera,
    retry,
    // Refs for components
    videoRef,
    canvasRef
  };
};
function ShareResultModal({
  open,
  onClose,
  repCount,
  xpEarned,
  username,
  tier
}) {
  const [copied, setCopied] = reactExports.useState(false);
  const shareText = `I just crushed ${repCount} push-ups on TeenTuffLifts and earned ${xpEarned} XP! 💪 Tier: ${tier} - Can you beat me? #TeenTuffLifts #NoGymNeeded`;
  const handleWhatsApp = () => {
    window.open(
      `https://wa.me/?text=${encodeURIComponent(shareText)}`,
      "_blank"
    );
  };
  const handleInstagram = async () => {
    if (navigator.share) {
      try {
        await navigator.share({
          text: shareText,
          title: "TeenTuffLifts Result"
        });
      } catch {
        await copyToClipboard();
        ue.info(
          "Text copied! Open Instagram Stories and paste your result 📸"
        );
      }
    } else {
      await copyToClipboard();
      ue.info(
        "Text copied! Open Instagram Stories and paste your result 📸",
        { duration: 5e3 }
      );
    }
  };
  const handleSnapchat = () => {
    const snapUrl = `https://www.snapchat.com/scan?attachmentUrl=${encodeURIComponent(shareText)}`;
    const snapWindow = window.open(snapUrl, "_blank");
    if (!snapWindow) {
      copyToClipboard();
      ue.info("Text copied! Open Snapchat and paste your result 👻");
    }
  };
  const copyToClipboard = async () => {
    try {
      await navigator.clipboard.writeText(shareText);
      setCopied(true);
      setTimeout(() => setCopied(false), 2e3);
      ue.success("Result copied to clipboard!");
    } catch {
      ue.error("Could not copy to clipboard");
    }
  };
  const tierCapitalized = tier.charAt(0).toUpperCase() + tier.slice(1);
  return /* @__PURE__ */ jsxRuntimeExports.jsx(Dialog, { open, onOpenChange: (v) => !v && onClose(), children: /* @__PURE__ */ jsxRuntimeExports.jsxs(
    DialogContent,
    {
      "data-ocid": "share.modal",
      className: "bg-card border-border max-w-sm p-0 overflow-hidden",
      children: [
        /* @__PURE__ */ jsxRuntimeExports.jsx(DialogHeader, { className: "px-5 pt-5 pb-0", children: /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center justify-between", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs(DialogTitle, { className: "font-display font-black text-xl flex items-center gap-2", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx(Share2, { className: "w-5 h-5 text-neon-green" }),
            "Share Your Result"
          ] }),
          /* @__PURE__ */ jsxRuntimeExports.jsx(
            Button,
            {
              "data-ocid": "share.close.button",
              variant: "ghost",
              size: "icon",
              onClick: onClose,
              className: "w-8 h-8 text-muted-foreground hover:text-foreground",
              children: /* @__PURE__ */ jsxRuntimeExports.jsx(X, { className: "w-4 h-4" })
            }
          )
        ] }) }),
        /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "px-5 pt-4 pb-5 space-y-4", children: [
          /* @__PURE__ */ jsxRuntimeExports.jsxs(
            "div",
            {
              className: "rounded-2xl p-5 relative overflow-hidden",
              style: {
                background: "linear-gradient(135deg, oklch(0.12 0.03 265), oklch(0.18 0.08 130 / 0.4))",
                border: "1px solid oklch(0.85 0.22 130 / 0.3)"
              },
              children: [
                /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "absolute top-0 right-0 text-6xl opacity-10 select-none pointer-events-none translate-x-1 -translate-y-1", children: "💪" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "text-xs font-body text-muted-foreground uppercase tracking-wider mb-1", children: "TeenTuffLifts Result" }),
                /* @__PURE__ */ jsxRuntimeExports.jsx(
                  "div",
                  {
                    className: "font-display font-black text-4xl text-neon-green leading-none mb-1",
                    style: { textShadow: "0 0 20px oklch(0.85 0.22 130 / 0.4)" },
                    children: repCount
                  }
                ),
                /* @__PURE__ */ jsxRuntimeExports.jsx("div", { className: "text-sm font-body text-muted-foreground mb-3", children: "Push-Ups Crushed" }),
                /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center justify-between", children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-1.5", children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-xs font-body text-muted-foreground", children: "by" }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "font-display font-bold text-sm text-foreground", children: username })
                  ] }),
                  /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "flex items-center gap-2", children: [
                    /* @__PURE__ */ jsxRuntimeExports.jsxs("span", { className: "text-xs bg-primary/20 text-neon-green border border-primary/30 rounded-full px-2 py-0.5 font-body font-medium", children: [
                      "+",
                      xpEarned,
                      " XP"
                    ] }),
                    /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-xs bg-chart-4/20 text-chart-4 border border-chart-4/30 rounded-full px-2 py-0.5 font-body font-medium", children: tierCapitalized })
                  ] })
                ] })
              ]
            }
          ),
          /* @__PURE__ */ jsxRuntimeExports.jsxs("div", { className: "space-y-2.5", children: [
            /* @__PURE__ */ jsxRuntimeExports.jsx("p", { className: "text-xs text-muted-foreground font-body text-center uppercase tracking-wider", children: "Share to" }),
            /* @__PURE__ */ jsxRuntimeExports.jsxs(
              Button,
              {
                "data-ocid": "share.whatsapp.button",
                onClick: handleWhatsApp,
                className: "w-full h-11 font-display font-bold text-sm justify-start gap-3",
                style: {
                  background: "oklch(0.45 0.18 145 / 0.2)",
                  border: "1px solid oklch(0.55 0.18 145 / 0.4)",
                  color: "oklch(0.75 0.2 145)"
                },
                children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx(MessageCircle, { className: "w-5 h-5" }),
                  "Share on WhatsApp"
                ]
              }
            ),
            /* @__PURE__ */ jsxRuntimeExports.jsxs(
              Button,
              {
                "data-ocid": "share.instagram.button",
                onClick: handleInstagram,
                className: "w-full h-11 font-display font-bold text-sm justify-start gap-3",
                style: {
                  background: "oklch(0.45 0.2 340 / 0.2)",
                  border: "1px solid oklch(0.6 0.22 340 / 0.4)",
                  color: "oklch(0.75 0.2 340)"
                },
                children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx(Instagram, { className: "w-5 h-5" }),
                  "Share to Instagram"
                ]
              }
            ),
            /* @__PURE__ */ jsxRuntimeExports.jsxs(
              Button,
              {
                "data-ocid": "share.snapchat.button",
                onClick: handleSnapchat,
                className: "w-full h-11 font-display font-bold text-sm justify-start gap-3",
                style: {
                  background: "oklch(0.7 0.22 90 / 0.15)",
                  border: "1px solid oklch(0.75 0.22 90 / 0.4)",
                  color: "oklch(0.85 0.2 90)"
                },
                children: [
                  /* @__PURE__ */ jsxRuntimeExports.jsx("span", { className: "text-lg leading-none", children: "👻" }),
                  "Share to Snapchat"
                ]
              }
            ),
            /* @__PURE__ */ jsxRuntimeExports.jsxs(
              Button,
              {
                "data-ocid": "share.copy.button",
                onClick: copyToClipboard,
                variant: "outline",
                className: "w-full h-11 font-display font-bold text-sm justify-start gap-3 border-border",
                children: [
                  copied ? /* @__PURE__ */ jsxRuntimeExports.jsx(Check, { className: "w-5 h-5 text-neon-green" }) : /* @__PURE__ */ jsxRuntimeExports.jsx(Copy, { className: "w-5 h-5" }),
                  copied ? "Copied!" : "Copy Result Text"
                ]
              }
            )
          ] })
        ] })
      ]
    }
  ) });
}
export {
  Camera as C,
  RefreshCw as R,
  Share2 as S,
  CircleAlert as a,
  RotateCcw as b,
  CircleCheckBig as c,
  ShareResultModal as d,
  Copy as e,
  useCamera as u
};
